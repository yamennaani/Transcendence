services:
  nginx:
    container_name: nginx

    build:
      context: .
      dockerfile: Dockerfile

    command: ["nginx", "-g", "daemon off;"]

    volumes:
      - ./conf.d:/etc/nginx/conf.d

    networks:
      - backend-network
      - frontend-network

    ports:
      - "80:80"
      - "443:443"

    depends_on:
      - user_service
      - auth_service
      - org_service
      - class_service
      - enroll_service
      - group_service
      - submission_service
      - eval_service
      - frontend

networks:

  backend-network:

  
  frontend-network:
