services:
  eval_service:
    container_name: eval_service
    build:
      context: ../..
      dockerfile: services/eval/Dockerfile
    env_file:
      - ../../.env
    environment:
      - DATABASE_URL=postgresql://${POSTGRES_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${POSTGRES_DB}
    ports:
      - "3008:3000"
    depends_on:
      - database
    networks:
      - database-network
      - backend-network

networks:
  database-network:

  backend-network:
